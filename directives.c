#include <stdio.h>
// #include "namefile"
#define PI 3.14
#define CUBO(a) a*a*a
int main(){
  // int suma;
  // suma = PI + 3;
  int a = 3;
  printf("El cubo de la variable a es : %i\n", CUBO(a));
  return 0;
}